let element = React.createElement(
  "h3",
  { style: { color: "red" } },
  "Hello world ! Ashu is Ready with full proof plan!"
);
let rootElement = ReactDOM.createRoot(document.getElementById(root));
rootElement.render(element);
